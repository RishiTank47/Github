#include <stdio.h>
#include <string.h>

int main()
{
    char str[10];
    
    gets(str);
    
   // printf("%d\n",strlen(str));
   // puts(struper(str));
    
    puts(strupr(str));
    
    return 0;
    
    
    
    
}
